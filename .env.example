# Oracle (OCI DB System / Autonomous). Thin mode: no Instant Client needed.
ORACLE_USER=ooci
ORACLE_PASSWORD=WElcome1234_#
ORACLE_CONNECT_STRING=10.18.0.7:1521/pdb1.dbprv.db.oraclevcn.com

# <slug>.BASE_DOMAIN resolves a site by slug; any other host resolves by custom_domain.
BASE_DOMAIN=pppp.p373r.net

# Token for /admin and POST /api/purge
ADMIN_TOKEN=change_me_long_random

# Local dev without Oracle: leave ORACLE_CONNECT_STRING empty and set a public Notion page id.
# DEV_ROOT_PAGE=067dd719a912471ea9a3ac10710e7fdf

# Page cache TTL (seconds). Stale pages are served while refreshing in background.
PAGE_TTL=60
